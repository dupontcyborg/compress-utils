# Generated corpus data (reproducible from generate.py + manifest.json).
corpus/data/

# Compiled drivers.
drivers/c/bench
drivers/c/bench_baseline

# Run outputs. Commit baselines explicitly with `git add -f`.
results/
